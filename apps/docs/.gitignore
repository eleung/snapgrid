/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# Generated by scripts/seo.mjs (prebuild)
/public/sitemap.xml
/public/robots.txt
/public/site.webmanifest

# Local dev search index (copied from out/_pagefind by `pnpm search:sync` so
# `next dev` can serve it at /_pagefind/). The prod build regenerates the index
# in out/ and strips this copy in prebuild, so it never ships.
/public/_pagefind/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/.playwright/
