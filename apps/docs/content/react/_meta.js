// The React binding's docs tree. `docs` is the guide/API section (its own
// sidebar), `examples` is the live-demo gallery. Both reachable via the
// framework-aware nav links; this file just fixes their order/titles.
export default {
  docs: "Documentation",
  examples: "Examples",
};
