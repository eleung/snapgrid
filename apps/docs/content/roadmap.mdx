---
title: Roadmap
description: Where snapgrid is headed. Bindings for Vue, Svelte, Solid and vanilla TypeScript, deeper dnd-kit interop with Droppable and Sortable, and a /legacy drop-in for react-grid-layout v1.
---

import { Callout } from "nextra/components";

# Roadmap

snapgrid sits on two framework-agnostic foundations: react-grid-layout's packing core and dnd-kit's
DOM engine. Today it ships as `@snapgridjs/react`, but neither the layout core nor the drag
engine contains any React, which is what makes the directions below possible.

<Callout type="info">
  This is direction, not a dated commitment. To weigh in or help build any of it, open an issue or
  PR on [GitHub](https://github.com/eleung/snapgrid).
</Callout>

## Framework bindings

dnd-kit is framework-agnostic by design: a vanilla DOM engine with thin per-framework bindings.
snapgrid is built the same way: `@snapgridjs/core` holds the layout math, compaction, and drag session
with no framework code, and `@snapgridjs/react` is a small binding on top. The same core can drive
others:

- **Vanilla TypeScript** (no framework at all).
- **Vue**
- **Svelte**
- **Solid**

Each binding owns only the rendering glue — refs, reactivity, lifecycle — while the layout behaviour
stays shared, so a grid behaves identically whichever framework renders it.

## Deeper dnd-kit interop

Because snapgrid speaks dnd-kit's drag protocol, a grid can exchange items with the rest of the
dnd-kit ecosystem, not just with other grids. Today an external dnd-kit **Draggable** can be dropped
into a grid (see the [external drop guide](/docs/guides/external-drop)). Next, to close the loop in
both directions:

- **Grid item → dnd-kit `Droppable`.** Drag a tile out of the grid and drop it on any dnd-kit drop
  target, not only another snapgrid.
- **Back-and-forth with dnd-kit `Sortable`.** Move items between a grid and a dnd-kit sortable list
  either way, with live placeholders and reordering on both sides.

The aim is for a snapgrid grid to be just another participant in a dnd-kit drag: items flow in and
out of lists, boards, and drop zones without bespoke glue code.

## A `/legacy` drop-in for react-grid-layout v1

snapgrid's `<GridLayout>` already mirrors **react-grid-layout v2** closely — for v2 users, migrating
is essentially swapping the import (see the comparison on the [home page](/)). But the large v1
installed base is still on the older `WidthProvider(GridLayout)` HOC and the flat `cols` / `rowHeight`
/ `draggableHandle` props, which don't map one-to-one.

The plan is a **`@snapgridjs/react/legacy`** entry that accepts the v1 prop shape and adapts it to
snapgrid — so a v1 codebase can change one import line, keep its existing props, and migrate
incrementally to the modern API at its own pace. This mirrors how RGL v2 itself ships a
`react-grid-layout/legacy` build for its own v1 → v2 transition.

- **`WidthProvider` HOC** → a compatible wrapper (or auto-measured width).
- **Flat props** (`cols`, `rowHeight`, `margin`, `draggableHandle`, `compactType`, …) → mapped onto
  snapgrid's `gridConfig` / `dragConfig` / `compactor`.
- Same controlled `onLayoutChange` contract, so layout state and persistence carry over untouched.

Until then, the [migration guide](/docs/guides/migrating-from-rgl) covers the prop-by-prop mapping by
hand.

## Shape the roadmap

These priorities follow what people actually build with snapgrid. Have a use case, or a framework you
need? [Open an issue](https://github.com/eleung/snapgrid/issues). It genuinely steers what comes next.
